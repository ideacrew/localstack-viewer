[package]
name = "localstack-viewer-server"
version = "0.1.0"
edition = "2024"

[dependencies]
rocket = "0.5.1"
serde_json = { version = "1.0.140", features = ["raw_value"] }
reqwest = { version = "0.12.22", features = ["blocking"] }
serde = { version = "1.0.219" }
